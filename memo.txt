1. thunk 함수를 구현
2. 리듀서 로직 구현: reducers (원래는 ReduxTK = todos.js모듈에서 todosSlice함수의 reducers에 => extraReducers로)
    - 서버 통신 : 100% 성공 X
    - 지금까지의 reduxx state(todos, counter)
    -> 앞으로의 state (isLoading, isError, data)
3. 기능 확인 (network) + devTools
4. Store의 값을 조회 + 화면에 렌더링